name: airplane_tracker
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_config: ^1.0.8
  http: ^0.12.2
  modal_progress_hud: ^0.1.3
  url_launcher: ^5.7.10
  intl: ^0.16.1
  get: ^3.24.0
  flutter_map: ^0.11.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
